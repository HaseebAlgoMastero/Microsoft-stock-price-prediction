# -*- coding: utf-8 -*-
"""Microsoft Stock Price Prediction Model

Automatically generated by Colaboratory.

Original file is located at
    https://colab.research.google.com/drive/1sbVLTSuXYSzH17cd5Wf3RqixkztEq-PU
"""

import numpy as np
import pandas as pd
import matplotlib.pyplot as plt
import plotly.graph_objects as go
import seaborn as sns
sns.set()
plt.style.use('fivethirtyeight')

data = pd.read_csv('MSFT.csv')

data.head()

figure = go.Figure(data = [go.Candlestick(x = data['Date'],
                                          open = data['Open'],
                                          high = data['High'],
                                          low = data['Low'],
                                          close = data['Close'])])
figure.update_layout(title = 'Microsoft Stock Price Prediction',
                     xaxis_rangeslider_visible = True)
figure.show()

data.corr()

print(data.corr())
sns.heatmap(data.corr())
plt.show()

x = data[['Open','High','Low']].values

y = data['Close'].values

from sklearn.model_selection import train_test_split
x_train,x_test,y_train,y_test = train_test_split(x,y,test_size =0.2,random_state = 0)

from sklearn.tree import DecisionTreeRegressor
regressor = DecisionTreeRegressor()
regressor.fit(x_train,y_train)

y_pred = regressor.predict(x_test)

y_pred

from sklearn.metrics import r2_score
score = r2_score(y_test,y_pred)
score

model = pd.DataFrame(data = {'Predicted stock values':y_pred})

model